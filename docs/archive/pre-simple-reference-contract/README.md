# Pre Simple Reference Contract Docs

This directory contains historical project-structure references written before the current BEV simple reference/control contract.

These files are preserved for archaeology only. They must not be used as active runtime, parameter, media, overlay, verification, or extension authority.

Current authority:

- Root `README.md` for BEV extension rules.
- `new/config/default_params.md` for runtime parameter contract.
- `new/code/port/README.md` for type/include boundaries.
- `new/user/README.md` for current build, deployment, and debug workflow.
