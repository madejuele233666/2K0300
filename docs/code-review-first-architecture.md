# Code-Review-First Architecture

## Status

This file is now a historical index, not the active Stage A architecture
contract.

The active verification model is `verification-cycle`:

- `openspec/schemas/modules/verification-cycle/README.md`
- `openspec/schemas/modules/verification-cycle/VERIFY-IMPLEMENTATION.md`
- `openspec/schemas/ai-enforced-workflow/verification-sequence.md`
- `openspec/schemas/ai-enforced-workflow/schema.yaml`

The full pre-`verification-cycle` architecture draft was archived at:

- `docs/review-flow/archive/code-review-first-architecture-2026-04-18/code-review-first-architecture.md`

## Why It Was Downgraded

The previous document described an earlier design direction for review
orchestration. It is still useful as design history, but it no longer matches
the active rules, contracts, or runtime surfaces.

Use the archived copy only for historical reasoning, not for current workflow
implementation.
