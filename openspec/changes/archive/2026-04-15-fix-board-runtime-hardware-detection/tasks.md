## 1. Truthful Board Smoke Verdict

- [x] 1.1 Update `new/user/run_remote_smoke.sh` so remote runtime exit status is preserved separately from log-copy status, local verification logs still capture remote output on failure, and local success means the board process itself succeeded.
- [x] 1.2 Record smoke-helper verification planning using shared sequence `verify-sequence/default` with minimal bundle `change/mode/risk_tier/evidence_paths_or_diff_scope/findings_contract/retry_policy`; invoke verifier review through built-in subagent API using template id `verify-reviewer-inline-v1`, write local review JSON to `openspec/changes/fix-board-runtime-hardware-detection/verification/artifact-local-review.json`, keep Gemini policy optional for this `STANDARD` change unless explicitly dual-gated, reserve runner contract `gemini-capture` raw/report paths under `verification/`, use fallback `input_raw_path -> report_path` if Gemini writes raw only, route artifact blockers to `openspec-repair-change`, and require a fresh verifier instance on every rerun.
- [x] 1.3 [Checkpoint] Run verifier-subagent local review for smoke-helper artifact changes using `verify-sequence/default`; write normalized local review JSON and ensure each rerun uses a fresh verifier instance (no inherited memory). If Gemini is required by risk tier or explicit dual gate, run logical runner contract `gemini-capture`, write both raw and normalized reports, and if primary execution fails after writing raw, run recovery (`input_raw_path -> report_path`) before blocking. Record verifier invocation metadata (`agent_id/start_at/end_at/final_state`) and Gemini resolved Linux/Windows command (when Gemini runs) in execution evidence, not in task policy prose.

## 2. Direct-Match Hardware Discovery And Failure Propagation

- [x] 2.1 Update `new/code/platform/true_ls2k0300/imu_bridge.cpp`, related bridge declarations, and `new/code/platform/imu_adapter.cpp` so direct-match IMU startup resolves a real board-visible node or explicit override path, reports the resolved resource in diagnostics, and refuses readiness when discovery fails.
- [x] 2.2 Update `new/code/platform/true_ls2k0300/encoder_bridge.cpp`, `motor_bridge.cpp`, `adc_bridge.cpp`, `new/code/platform/encoder_adapter.cpp`, `motor_adapter.cpp`, and `power_adapter.cpp` so bridge read/write failures are propagated as invalid/not-ready states instead of unconditional success, synthetic valid samples, or fabricated low-voltage values.
- [x] 2.3 Refresh any touched path constants, bridge result types, and startup/control-loop callers so direct-match readiness and fail-safe veto semantics stay coherent across `new/code/platform/`, `new/code/runtime/startup.cpp`, and diagnostics output.
- [x] 2.4 [Checkpoint] Run verifier-subagent local review for bridge/adapter contract changes using `verify-sequence/default`; write normalized local review JSON and ensure each rerun uses a fresh verifier instance (no inherited memory). If Gemini is required by risk tier or explicit dual gate, run logical runner contract `gemini-capture`, write both raw and normalized reports, and if primary execution fails after writing raw, run recovery (`input_raw_path -> report_path`) before blocking. Record verifier invocation metadata (`agent_id/start_at/end_at/final_state`) and Gemini resolved Linux/Windows command (when Gemini runs) in execution evidence, not in task policy prose.

## 3. Board Verification Evidence

- [x] 3.1 Rebuild with `new/user/build.sh`, execute a controlled failing board-startup path or equivalent fault injection through `new/user/run_remote_smoke.sh`, and capture evidence under `openspec/changes/fix-board-runtime-hardware-detection/verification/` proving the helper returned non-zero locally while still copying the remote runtime log and command-resolution evidence.
- [x] 3.2 Rerun board smoke with `new/user/run_remote_smoke.sh` for the intended direct-match startup path, and capture refreshed runtime smoke logs plus hardware-discovery diagnostics that explicitly identify IMU discovery result, camera path, and encoder/motor/ADC resource evidence needed to explain success or fail-safe startup under `openspec/changes/fix-board-runtime-hardware-detection/verification/`.
- [x] 3.3 Plan implementation verification with shared sequence `verify-sequence/default` using minimal bundle `change/mode/risk_tier/evidence_paths_or_diff_scope/findings_contract/retry_policy`; invoke verifier review through built-in subagent API using template id `verify-reviewer-inline-v1`, write normalized local review JSON to `openspec/changes/fix-board-runtime-hardware-detection/verification/implementation-local-review.json`, keep Gemini optional unless explicitly dual-gated before archive, reserve `gemini-capture` raw/report paths under `verification/`, require fallback `input_raw_path -> report_path` if Gemini raw exists without report output, route non-implementation blockers to `openspec-repair-change`, and require fresh verifier instances on every rerun.
- [x] 3.4 [Checkpoint] Run verifier-subagent local review for implementation verification evidence using `verify-sequence/default`; write normalized local review JSON and ensure each rerun uses a fresh verifier instance (no inherited memory). If Gemini is required by risk tier or explicit dual gate, run logical runner contract `gemini-capture`, write both raw and normalized reports, and if primary execution fails after writing raw, run recovery (`input_raw_path -> report_path`) before blocking. Record verifier invocation metadata (`agent_id/start_at/end_at/final_state`) and Gemini resolved Linux/Windows command (when Gemini runs) in execution evidence, not in task policy prose.
