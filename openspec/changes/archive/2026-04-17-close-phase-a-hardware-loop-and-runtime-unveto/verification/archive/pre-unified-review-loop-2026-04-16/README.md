# Pre-Unified Review-Loop Archive

This archive preserves the change-local verification outputs that were written
before the shared review loop was updated to the current
`review_goal=implementation_correctness` plus
`review_phase=docs_first|source_first` model.

Archived here:

- root summary files that used `artifact` / `artifact_readiness`
- review runs that treated the change bundle as `artifacts-as-implementation`

These files are historical trace only.

Do not use them as authoritative evidence for the current workflow.

Current authoritative guidance for this change now lives in:

- `../../README.md`
- `../../design.md`
- `../../tasks.md`
