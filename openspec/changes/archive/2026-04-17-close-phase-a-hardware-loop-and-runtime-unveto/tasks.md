## 0. Verification Contract

- Shared sequence:
  - `openspec/schemas/ai-enforced-workflow/verification-sequence.md#verify-sequence/default`
- Shared JSON verification contract:
  - `openspec/schemas/modules/review-loop/contracts/review-loop-core-v1.json`
  - `openspec/schemas/modules/review-loop/contracts/review-loop-openspec-adapter-v1.json`
- Cache-helper sequence:
  - `openspec/schemas/ai-enforced-workflow/index-sequence.md#index-sequence/default`
- Shared field groups:
  - `bundle_required`
  - `index_context_optional`
  - `output_paths_required`
  - `output_paths_optional`
  - `verifier_evidence_required`
  - `implementation_evidence_required`
  - `challenger_entry.source_review_required`
- Shared semantic rules:
  - `review_goal=implementation_correctness`
  - `review_phase=docs_first|source_first`
  - ordinary reruns stay in the same working session
  - challenger must be fresh
  - challenger findings promote that challenger session into the next working baseline through `reopen-record.json`
- Transition authority:
  - use the shared transition resolver
  - execute the returned decision exactly
  - do not improvise challenger entry or closure from prompt prose
- Routing target for blocking findings:
  - `openspec-repair-change`
- Supported continuation overrides:
  - `verify-only`
  - `dry-run`
  - `manual_pause`

## 2. Freeze The Phase A Starting Point

- [x] 2.1 Create `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/phase-a-baseline.md` that references the accepted `2026-04-15` direct-match evidence bundle and states exactly which startup-grade facts are already accepted versus which Phase A blockers remain open.
- [x] 2.2 Normalize change-local evidence naming for the remaining checkpoints, including target outputs for `phase-a-imu-closure.{log,md}`, `phase-a-encoder-closure.{log,md}`, `phase-a-motor-closure.{log,md}`, `phase-a-control-unveto.{log,md}`, and `phase-a-camera-exposure.{log,md}`.
- [x] 2.3 Update the relevant Phase A docs in `new/docs/race-finish-series.zh-CN/` so they treat the accepted baseline as the entry point for later checkpoint evidence instead of re-arguing direct-match startup.

## 3. Close Sensor Trust Before Control Claims

- [x] 3.1 Implement any IMU bridge/adapter-localized fixes needed to make direct-match IMU runtime samples continuously usable without leaking vendor details outside the owning boundary, then capture `phase-a-imu-closure.log` and `phase-a-imu-closure.md` with static-sample, continuity, and direction evidence.
- [x] 3.2 Implement any encoder bridge/adapter-localized fixes needed to make delta baseline, jump handling, sign interpretation, and magnitude explanation trustworthy, then capture `phase-a-encoder-closure.log` and `phase-a-encoder-closure.md`.
- [x] 3.3 Update Phase A docs and evidence notes so IMU startup proof stays separate from IMU sample closure, and encoder “can read” stays separate from encoder “trusted for control”.
- [ ] 3.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the sensor-closure source bundle (`new/code/platform/imu_adapter.cpp`, `new/code/platform/true_ls2k0300/imu_bridge.cpp`, `new/code/platform/encoder_adapter.cpp`, `new/code/platform/true_ls2k0300/encoder_bridge.cpp`, directly impacted docs, and `phase-a-imu-closure.*` / `phase-a-encoder-closure.*`) as a `source_first` checkpoint. Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/review-loop/contracts/review-loop-core-v1.json` and `openspec/schemas/modules/review-loop/contracts/review-loop-openspec-adapter-v1.json` for `bundle_required`, `index_context_optional`, `output_paths_required`, `output_paths_optional`, `verifier_evidence_required`, and `implementation_evidence_required`, plus the routing target and any caller-local guardrails. Require `agent-spawn-decision-v1` before any new verifier or index-maintainer spawn, with the initial working-pass record at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/sensor-closure/working/attempt-1/spawn-decision.json`, any exception-only fresh working record at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/sensor-closure/working/attempt-<n>/spawn-decision.json`, and any challenger-driven promotion recorded at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/sensor-closure/working/attempt-<n>/reopen-record.json`. Require authoritative verifier findings JSON at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/implementation-working-findings.sensors.json` plus verifier execution evidence JSON at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/implementation-working-evidence.sensors.json`. Require inline implementation evidence using `implementation_evidence_required`, including `review_scope` and `review_coverage`. Use `openspec-verify-change` through the shared transition resolver rather than prompt-local transition logic.

## 4. Close Actuator Trust And Diagnose Control-Unlock Blockers

- [x] 4.1 Implement any motor-path fixes that are required to keep real output, emergency-stop rollback, and write-failure fail-safe semantics localized to the platform-owned motor boundary, then capture `phase-a-motor-closure.log`, `phase-a-motor-closure.md`, and any safe bench evidence required by the design.
- [x] 4.2 Implement any runtime-owned observability refinements needed to explain sustained veto versus sustained non-veto intervals without leaking vendor details, then capture an initial `phase-a-control-unveto.log` and `phase-a-control-unveto.md` that classify dominant veto causes, `control.command.requested_nonzero`, `control.apply.*`, and `control.arm.transition`.
- [x] 4.3 If the dominant blocker profile in `phase-a-control-unveto.*` is perception freshness or `perception_emergency`, promote exposure-policy resolution before any final “stable control unlock” claim; otherwise keep exposure as a downstream closure decision and record that rationale in the same evidence note.
- [x] 4.4 Update Phase A docs and evidence notes so “motor write succeeded” is not treated as equivalent to “physical actuator path is closed”, and so preliminary control-unlock diagnostics are tied back to named blockers rather than marker presence alone.
- [ ] 4.5 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the actuator/control source bundle (`new/code/platform/motor_adapter.cpp`, `new/code/platform/true_ls2k0300/motor_bridge.cpp`, `new/code/runtime/control_loop.cpp`, `new/code/runtime/control_decision.*`, directly impacted docs, and `phase-a-motor-closure.*` / initial `phase-a-control-unveto.*`) as a `source_first` checkpoint. Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/review-loop/contracts/review-loop-core-v1.json` and `openspec/schemas/modules/review-loop/contracts/review-loop-openspec-adapter-v1.json` for `bundle_required`, `index_context_optional`, `output_paths_required`, `output_paths_optional`, `verifier_evidence_required`, and `implementation_evidence_required`, plus the routing target and any caller-local guardrails. Require `agent-spawn-decision-v1` before any new verifier or index-maintainer spawn, with the initial working-pass record at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/actuator-control/working/attempt-1/spawn-decision.json`, any exception-only fresh working record at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/actuator-control/working/attempt-<n>/spawn-decision.json`, and any challenger-driven promotion recorded at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/actuator-control/working/attempt-<n>/reopen-record.json`. Require authoritative verifier findings JSON at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/implementation-working-findings.control.json` plus verifier execution evidence JSON at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/implementation-working-evidence.control.json`. Require inline implementation evidence using `implementation_evidence_required`, including `review_scope` and `review_coverage`. Use `openspec-verify-change` through the shared transition resolver rather than prompt-local transition logic.

## 5. Decide Exposure Policy And Write The Phase A Exit Judgment

- [x] 5.1 Determine whether the direct-match camera path truly supports exposure control; if it does not, document the current mainline policy as a bounded Phase A decision rather than an open-ended future concern, and capture `phase-a-camera-exposure.log`, `phase-a-camera-exposure.md`, and any `phase-a-camera-samples.md` needed to support the conclusion. If task `4.3` promoted exposure because perception/exposure dominated the blocker profile, rerun and refresh `phase-a-control-unveto.*` after this decision before claiming final control unlock status.
- [x] 5.2 Update `new/docs/race-finish-series.zh-CN/01-phase-a-hardware-and-closure.md`, `00-master-roadmap.md`, and `07-current-progress.md` so they explicitly state which Phase A exit conditions are met, which blockers remain if any, whether exposure stayed downstream or was promoted earlier as the active blocker, and whether entry into `Phase B` is allowed.
- [x] 5.3 Write a concise change-local closure summary in `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/phase-a-exit-judgment.md` that consolidates the baseline note, sensor closure, actuator closure, final control unlock status, and exposure decision into one stage decision record.
- [x] 5.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the final source bundle (all changed code, all changed Phase A docs, refreshed `phase-a-control-unveto.*`, `phase-a-camera-exposure.*`, optional sample analysis, and `phase-a-exit-judgment.md`) as a `source_first` checkpoint. Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/review-loop/contracts/review-loop-core-v1.json` and `openspec/schemas/modules/review-loop/contracts/review-loop-openspec-adapter-v1.json` for `bundle_required`, `index_context_optional`, `output_paths_required`, `output_paths_optional`, `verifier_evidence_required`, and `implementation_evidence_required`, plus the routing target and any caller-local guardrails. Require `agent-spawn-decision-v1` before any new verifier or index-maintainer spawn, with the initial working-pass record at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/final-phase-a-closure/working/attempt-1/spawn-decision.json`, any exception-only fresh working record at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/final-phase-a-closure/working/attempt-<n>/spawn-decision.json`, and any challenger-driven promotion recorded at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/final-phase-a-closure/working/attempt-<n>/reopen-record.json`. Require authoritative verifier findings JSON at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/implementation-working-findings.final.json` plus verifier execution evidence JSON at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/implementation-working-evidence.final.json`. Require inline implementation evidence using `implementation_evidence_required`, including `review_scope` and `review_coverage`. Use `openspec-verify-change` through the shared transition resolver rather than prompt-local transition logic.

## 6. Challenger Closure And Follow-On Routing

- [x] 6.1 After the orchestrator validates the latest converged working review outputs and confirms `challenger_entry.source_review_required`, authorize challenger entry for the current final source bundle; record the authorization decision and challenger `agent-spawn-decision-v1` at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/final-phase-a-closure/challenger/attempt-<n>/spawn-decision.json`.
- [x] 6.2 Run the challenger review for this change using `verify-sequence/default`, `verify-reviewer-inline-v2`, optional `index-sequence/default` support only when useful, and authoritative outputs at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/final-phase-a-closure/challenger/attempt-<n>/findings.json` plus `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/final-phase-a-closure/challenger/attempt-<n>/verifier-evidence.json`. Mirror the final authoritative summary paths at `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/implementation-challenger-findings.final.json` and `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/implementation-challenger-evidence.final.json`.
- [x] 6.3 If challenger returns blocking findings, write `openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/final-phase-a-closure/working/attempt-<n>/reopen-record.json`, bind it to the failed challenger outputs, promote that challenger `agent_id` into the next working baseline, route findings through `openspec-repair-change`, and continue reruns only in the promoted working session unless an explicit recovery exception later applies.
- [x] 6.4 If challenger returns zero findings with `closure_authority=challenger_confirmed`, validate the full review run with `python3 openspec/schemas/modules/review-loop/bin/review_loop_guard.py --run-dir openspec/changes/close-phase-a-hardware-loop-and-runtime-unveto/verification/review-runs/final-phase-a-closure`, then archive this change or create the next Phase B proposal based on the explicit `phase-a-exit-judgment.md` result.
