## 0. Verification Contract

- Shared sequence:
  - `openspec/schemas/ai-enforced-workflow/verification-sequence.md#verify-sequence/default`
- Shared JSON verification contract:
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json`
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json`
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-agent-table-v1.json`
- Shared field groups:
  - `invocation_common_required`
  - `output_paths_required`
  - `verifier_evidence_required`
  - `valid_pass_requirements`
  - `partial_scope_rule`
- Routing target for blocking findings:
  - `openspec-repair-change`
- Supported continuation overrides:
  - `verify-only`
  - `dry-run`
  - `manual_pause`
- Artifact-completion gate ownership:
  - when this task list completes the schema's `applyRequires` set under `ai-enforced-workflow`, the active artifact-creation caller (`openspec-propose` or `openspec-continue-change`) runs docs-first review before implementation entry
  - `openspec-apply-change` does not own that docs-first artifact gate

## 1. BEV Geometry And Control Cutover Foundation

- [x] 1.1 Add BEV-first shared types in `new/code/port/control_types.hpp` for `BEVTrackEstimate`, `BEVSceneObservation`, `VehicleContext`, `ControlConstraintSet`, `ControlErrorModel` inputs/outputs, compatibility projection fields, and runtime-owned FSM/reference/controller state, and mark old `LaneMetrics`/bottom-tracker-facing fields as removed or compatibility-projection-only surfaces.
- [x] 1.2 Implement `steering_bev_projector.*`, `steering_bev_geometry.*`, and `steering_observation_assembly.*` in `new/code/legacy/` so raw camera frames produce calibrated sparse BEV sampled edges, fixed forward-distance sampled path points, lane-width profile, visible-range/confidence summaries, and typed scene observations without relying on ordinary image-row ownership or old-chain shadow authority.
- [x] 1.3 Update `new/code/platform/param_store.cpp`, runtime parameter JSON handling, and steering-media `config_snapshot` surfaces so projector calibration, BEV geometry tuning, FSM thresholds, and control-model tuning have explicit startup-loaded ownership, and preserve an accepted calibration artifact/table that explains the board-side projector values.
- [x] 1.4 Implement `steering_control_error_model.*` and update `new/code/legacy/camera_logic.*`, `new/code/runtime/perception_frontend.*`, `new/code/legacy/pid_control.*`, and `new/code/runtime/control_loop.*` so raw frame -> BEV geometry -> BEV-native control error -> steering controller becomes the first authoritative runtime chain, while preserving the existing `turn_output -> wheel_target_mixer -> wheel PID -> PWM` terminal chain and routing all BEV/control tuning values through runtime parameters, accepted calibration artifacts, or documented compile-time invariants.
- [ ] 1.5 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the BEV geometry/control cutover foundation, explicit calibration/tuning ownership, typed observation assembly, sparse sampler evidence, and direct no-old-chain authority boundary. Reference field groups in `verification-cycle-core-v1.json` and `verification-cycle-openspec-adapter-v1.json`, require `review_goal=implementation_correctness`, follow `cycle_rules` for agent lifecycle, and require authoritative findings JSON, verifier evidence JSON, and caller/orchestrator-maintained `agent-table.json` at `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-1/attempt-<n>/findings.json`, `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-1/attempt-<n>/verifier-evidence.json`, and `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-1/agent-table.json`. Caller-local guardrails: compatibility projection must remain read-only, geometry/control modules must not reintroduce bottom-tracker or legacy pixel-chain authority, projector calibration must be reconstructable from accepted artifacts, CMake runtime/helper targets must include the new BEV/control modules, BEV/control thresholds, weights, sample selectors, and constraint limits must not appear as undocumented magic numbers, and findings route to `openspec-repair-change`.

## 2. Unified Scene FSM And Reference Policy

- [x] 2.1 Replace `legacy/steering_scene_orchestrator.*` and scattered scene progression rules with `steering_scene_fsm.*` and `steering_reference_policy.*`, preserving circle entry/interior/exit semantics while removing `active_module=special_wide` from the formal runtime contract.
- [x] 2.2 Rewrite circle/cross/zebra/ordinary-bend evidence consumers so they read only `BEVSceneObservation` and `VehicleContext`, and add selftests covering ordinary bend veto, cross whitelist, latched circle progression, release conditions, and the rule that scene phases change reference policy rather than directly generating motor commands.
- [x] 2.3 Update runtime-owned scene state in `new/code/runtime/runtime_state.hpp` and related exports so candidate/confirm/progress/release state is explicit, debug-only candidate traces are separated from formal module ownership, and `special_wide` public semantics are deleted rather than renamed.
- [ ] 2.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the FSM/reference-policy split, scene-state ownership, and `special_wide` deletion boundary. Reference field groups in `verification-cycle-core-v1.json` and `verification-cycle-openspec-adapter-v1.json`, require `review_goal=implementation_correctness`, follow `cycle_rules` for agent lifecycle, and require authoritative findings JSON, verifier evidence JSON, and caller/orchestrator-maintained `agent-table.json` at `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-2/attempt-<n>/findings.json`, `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-2/attempt-<n>/verifier-evidence.json`, and `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-2/agent-table.json`. Caller-local guardrails: FSM owns stage only, reference policy owns path selection, and no scene module may bypass the shared geometry/control entry.

## 3. Control Constraints, Confidence Behavior, And Accelerated Legacy Removal

- [x] 3.1 Harden `ControlConstraintSet` integration and BEV confidence/fallback behavior so low visible range, low track confidence, calibration invalidity, or sampled-path discontinuity degrades through accepted constraints, steering suppression, speed limits, or fail-safe gating rather than legacy pixel-chain fallback.
- [x] 3.2 Remove bottom tracker and old `LaneMetrics` from ordinary formal ownership, demote any surviving `highest_line/farthest_line/steering_reference_col` logic to explicit compatibility/debug/test-only adapters, and delete or quarantine any runtime gain-scheduling use of legacy image-space fields with a named removal target.
- [x] 3.3 Add reset-path, multi-cycle control, confidence-degradation, and future-provider boundary tests proving runtime-owned state separates BEV track carry-over, FSM state, reference-policy memory, and controller memory, and proving new control-affecting observation providers must enter through `ControlConstraintSet` rather than editing PID branches directly.
- [ ] 3.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for constraint/fail-safe behavior, runtime-owned reset boundary, future-provider boundary, and accelerated removal of legacy ordinary ownership. Reference field groups in `verification-cycle-core-v1.json` and `verification-cycle-openspec-adapter-v1.json`, require `review_goal=implementation_correctness`, follow `cycle_rules` for agent lifecycle, and require authoritative findings JSON, verifier evidence JSON, and caller/orchestrator-maintained `agent-table.json` at `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-3/attempt-<n>/findings.json`, `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-3/attempt-<n>/verifier-evidence.json`, and `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-3/agent-table.json`. Caller-local guardrails: compatibility fields must not own control behavior, future providers must use typed constraints, low-confidence BEV must not fall back to legacy pixel-chain authority, and blocking findings route to `openspec-repair-change`.

## 4. Observability, Tooling, And Board Evidence

- [x] 4.1 Update `new/code/runtime/control_debug_snapshot.hpp`, `new/code/platform/steering_media_protocol.*`, `new/code/platform/assistant_protocol.*`, and related runtime exporters so steering snapshots expose BEV-first primary fields, clear scene/reference ownership, and only optional compatibility projection fields.
- [x] 4.2 Update `new/user/steering_media_capture.py`, `new/user/tune_steering.py`, `new/user/scene_overlay_probe.cpp`, `new/user/steering_media_selftest.cpp`, and related tooling so host evidence distinguishes primary BEV steering fields from compatibility fields, supports raw `320x240` frames, and can render/check both raw-image and BEV overlays.
- [ ] 4.3 Run board smoke tests and preserved evidence capture for the BEV-first path, including build/deploy, representative straight/bend/circle/cross runs, steering-media alignment output, and a documented evidence bundle showing that ordinary control, scene stage, and reference policy all come from the same BEV geometry truth without old-chain comparison authority.
- [ ] 4.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for BEV-first observability, host tooling, and on-board smoke evidence. Reference field groups in `verification-cycle-core-v1.json` and `verification-cycle-openspec-adapter-v1.json`, require `review_goal=implementation_correctness`, follow `cycle_rules` for agent lifecycle, and require authoritative findings JSON, verifier evidence JSON, and caller/orchestrator-maintained `agent-table.json` at `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-4/attempt-<n>/findings.json`, `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-4/attempt-<n>/verifier-evidence.json`, and `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-4/agent-table.json`. Caller-local guardrails: board smoke must not rely on assistant-only interpretation, evidence must separate primary and compatibility surfaces, and blocking findings route to `openspec-repair-change`.

## 5. Final Source-First Verification And Implementation Entry

- [ ] 5.1 Run `openspec-verify-change` for the completed implementation bundle, covering changed `port/legacy/runtime/platform/new/user` surfaces, changed tests, preserved board evidence, and the deletion of formal `special_wide` and old ordinary geometry ownership.
- [ ] 5.2 Preserve final source-first verification artifacts at `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-5/`, including authoritative findings JSON, verifier evidence JSON, and current-state-only `agent-table.json`, and route any blocking findings to `openspec-repair-change`.
- [ ] 5.3 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the final source-first implementation bundle. Reference field groups in `verification-cycle-core-v1.json` and `verification-cycle-openspec-adapter-v1.json`, require `review_goal=implementation_correctness`, follow `cycle_rules` for agent lifecycle, and require authoritative findings JSON, verifier evidence JSON, and caller/orchestrator-maintained `agent-table.json` at `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-5/attempt-<n>/findings.json`, `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-5/attempt-<n>/verifier-evidence.json`, and `openspec/changes/bev-steering-perception-refactor/verification/checkpoint-5/agent-table.json`. Caller-local guardrails: final pass must show BEV single-truth ownership, no `special_wide` formal surface, no compatibility-field control authority, and complete coverage for changed code, changed tests, and directly impacted code.
