## 0. Verification Contract

- Shared sequence:
  - `openspec/schemas/ai-enforced-workflow/verification-sequence.md#verify-sequence/default`
- Shared JSON verification contract:
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json`
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json`
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-agent-table-v1.json`
- Cache-helper sequence:
  - `openspec/schemas/ai-enforced-workflow/index-sequence.md#index-sequence/default`
- Shared field groups:
  - `invocation_common_required`
  - `output_paths_required`
  - `verifier_evidence_required`
  - `valid_pass_requirements`
  - `partial_scope_rule`
- Routing target for blocking findings:
  - `openspec-repair-change`
- Supported continuation overrides:
  - `verify-only`
  - `dry-run`
  - `manual_pause`
- Artifact-completion gate ownership:
  - when this task list completes the schema's `applyRequires` set under `ai-enforced-workflow`, the active artifact-creation caller (`openspec-propose`, `openspec-ff-change`, or `openspec-continue-change`) runs docs-first review before implementation entry
  - `openspec-apply-change` does not own that docs-first artifact gate

## 1. Motion Lifecycle Contracts

- [x] 1.1 Add project-owned Phase B motion contract files under `new/code/runtime/` such as `motion_types.hpp` and `motion_supervisor.hpp/.cpp`, defining `MotionPhase`, `MotionIntent`, `MotionDecision`, supervisor inputs, and transition rules that adapt `old_2/project/user/main.cpp`, `old_2/project/code/isr.cpp`, and `old_2/project/code/camera.h` without copying BLDC-specific state names into the accepted `new/` public contract. Use `openspec-align` evidence from the reference files to keep the adaptation boundary explicit.
- [x] 1.2 Extend `new/code/runtime/runtime_state.hpp`, `new/code/port/control_types.hpp`, `new/code/platform/param_store.cpp`, and `new/config/default_params.json` with the Phase B lifecycle state and tuning parameters required by the design: clean gate confirmation, normative spinup completion timing, spinup turn clamp, PWM slew limit, stop timing, stop threshold, and fault re-arm hold.
- [x] 1.3 Keep lifecycle ownership above adapters by documenting and enforcing that `new/code/platform/*` remains vendor-facing normalization only, while `new/code/runtime/*` owns motion intent, phase transitions, hold-disarmed policy, and post-fault re-arm rules.
- [x] 1.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the motion lifecycle contracts and parameter surface (`new/code/runtime/motion_*`, `new/code/runtime/runtime_state.hpp`, `new/code/port/control_types.hpp`, `new/code/platform/param_store.cpp`, `new/config/default_params.json`). Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json` and `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json` for `invocation_common_required`, `output_paths_required`, `verifier_evidence_required`, `valid_pass_requirements`, and `partial_scope_rule`, plus the routing target and the caller-local guardrail that lifecycle ownership must remain in project-owned runtime code rather than adapters. Require authoritative verifier findings JSON at `openspec/changes/implement-phase-b-motion-lifecycle/verification/checkpoint-1/<pass>/findings.json`, verifier execution evidence JSON at `openspec/changes/implement-phase-b-motion-lifecycle/verification/checkpoint-1/<pass>/verifier-evidence.json`, and `agent-table.json` at `openspec/changes/implement-phase-b-motion-lifecycle/verification/checkpoint-1/agent-table.json`. State that usable active agents are resumed first, new agents are spawned only when no usable active agent exists, only `block -> pass` marks an agent `non_active`, and termination depends on a valid active pass.

## 2. Control Loop Integration And Reset Boundaries

- [x] 2.1 Update `new/code/runtime/control_decision.hpp/.cpp` and `new/code/runtime/control_loop.cpp` so runtime evaluation follows `sample -> EvaluateControlGate -> MotionSupervisor::Evaluate -> shaping/apply -> observation`, and add a distinct hold-disarmed apply outcome that does not overload `control.veto.*` or `control.apply.emergency_stop`.
- [x] 2.2 Add controller and carry-over reset support to `new/code/legacy/pid_control.*`, `new/code/legacy/attitude_logic.*`, and the `RuntimeState::W_Target_last` integration path so `DISARMED -> SPINUP`, `RUNNING/SPINUP -> FAIL_SAFE_LATCHED`, and `STOPPING -> DISARMED` begin from documented clean-state boundaries.
- [x] 2.3 Add project-owned diagnostics and observability markers for `motion.phase.transition`, `motion.start.blocked`, `motion.spinup.*`, `motion.stop.*`, `motion.failsafe.*`, and `control.apply.hold_disarmed`, while preserving existing `control.veto.*`, `control.apply.drive`, `control.apply.emergency_stop`, and `control.arm.transition` semantics.
- [x] 2.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the gate/lifecycle/apply integration and reset behavior (`new/code/runtime/control_loop.cpp`, `new/code/runtime/control_decision.*`, `new/code/legacy/pid_control.*`, `new/code/legacy/attitude_logic.*`). Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json` and `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json` for `invocation_common_required`, `output_paths_required`, `verifier_evidence_required`, `valid_pass_requirements`, and `partial_scope_rule`, plus the routing target and the caller-local guardrail that hold-disarmed and fail-safe remain distinct runtime outcomes. Require authoritative verifier findings JSON at `openspec/changes/implement-phase-b-motion-lifecycle/verification/checkpoint-2/<pass>/findings.json`, verifier execution evidence JSON at `openspec/changes/implement-phase-b-motion-lifecycle/verification/checkpoint-2/<pass>/verifier-evidence.json`, and `agent-table.json` at `openspec/changes/implement-phase-b-motion-lifecycle/verification/checkpoint-2/agent-table.json`. State that usable active agents are resumed first, new agents are spawned only when no usable active agent exists, only `block -> pass` marks an agent `non_active`, and termination depends on a valid active pass.

## 3. Product Exit Boundary, Test Harness, And Fault Injection

- [x] 3.1 Update `new/user/main.cpp` so product runtime behavior splits motion stop requests from process exit: signals and operator stop paths must request controlled stop first, wait for `DISARMED`, and only then terminate the process. The same entrypoint MUST also own the baseline non-test fault-reset trigger by mapping `SIGUSR1` to the one-shot `reset_fault_requested` motion intent without treating that reset as an exit path or as harness automation.
- [x] 3.2 Keep bounded automation as test harness policy by updating `new/user/run_remote_smoke.sh` and the runtime env surface so `LS2K_AUTO_START`, `LS2K_AUTO_START_DELAY_MS`, `LS2K_AUTO_STOP_AFTER_MS`, bounded frame-limit budgets, and `LS2K_AUTO_RESET_FAULT` remain optional, removable verification helpers rather than permanent runtime semantics. The harness-owned frame-limit path MUST request controlled stop and wait for `DISARMED` instead of remaining an implicit immediate-exit runtime path, and `run_remote_smoke.sh` MUST preserve the remote runtime exit status as the local smoke verdict after log retrieval. The wrapper/evidence bundle MUST also record the actual injected automation and fault-injection inputs as harness context separate from runtime markers.
- [x] 3.3 Add whitelist-based fault-injection hooks on the accepted runtime entrypoint by updating `new/code/runtime/perception_frontend.cpp`, `new/code/platform/imu_adapter.cpp`, `new/code/platform/encoder_adapter.cpp`, and the existing `new/code/platform/power_adapter.cpp` low-voltage override path for repeatable B-5 drop-frame / IMU-invalid / encoder-invalid / low-voltage exercises.
- [x] 3.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for `main.cpp`, `run_remote_smoke.sh`, and the Phase B fault-injection/runtime-boundary updates. Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json` and `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json` for `invocation_common_required`, `output_paths_required`, `verifier_evidence_required`, `valid_pass_requirements`, and `partial_scope_rule`, plus the routing target and the caller-local guardrail that test automation must remain separate from long-lived product semantics. Require authoritative verifier findings JSON at `openspec/changes/implement-phase-b-motion-lifecycle/verification/checkpoint-3/<pass>/findings.json`, verifier execution evidence JSON at `openspec/changes/implement-phase-b-motion-lifecycle/verification/checkpoint-3/<pass>/verifier-evidence.json`, and `agent-table.json` at `openspec/changes/implement-phase-b-motion-lifecycle/verification/checkpoint-3/agent-table.json`. State that usable active agents are resumed first, new agents are spawned only when no usable active agent exists, only `block -> pass` marks an agent `non_active`, and termination depends on a valid active pass.

## 4. Phase B Documents, Evidence, And Change Verification

- [x] 4.1 Update `new/docs/race-finish-series.zh-CN/02-phase-b-low-speed-vehicle-motion.md` and any directly impacted roadmap/progress references so Phase B tasks, acceptance markers, and evidence expectations align with the implemented motion lifecycle instead of assuming "car can move" is enough.
- [x] 4.2 Produce implementation-grade verification notes and command examples for B-1 through B-5, including half-load, straight-run, turn-run, and fault-injection evidence paths under `new/verification/` or `openspec/changes/implement-phase-b-motion-lifecycle/verification/`. Each note MUST name the exact command or env contract used, enumerate the expected motion/control markers, capture or reference the harness-context block that records injected automation/fault inputs, and state what the resulting artifact proves versus what it does not prove.
- [x] 4.3 Use `openspec-verify-change` after implementation to run source-first review and record authoritative findings/evidence for the changed runtime surface, with active-agent resume/spawn semantics, valid-pass termination, and optional Gemini dual review only if repository policy explicitly enables it.
- [x] 4.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the implemented Phase B docs/evidence contract and final changed source surface. Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json` and `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json` for `invocation_common_required`, `output_paths_required`, `verifier_evidence_required`, `valid_pass_requirements`, and `partial_scope_rule`, plus the routing target and the caller-local guardrail that Phase B evidence must explain start, spinup, running, stop, and fail-safe recovery separately. Require authoritative verifier findings JSON at `openspec/changes/implement-phase-b-motion-lifecycle/verification/checkpoint-4/<pass>/findings.json`, verifier execution evidence JSON at `openspec/changes/implement-phase-b-motion-lifecycle/verification/checkpoint-4/<pass>/verifier-evidence.json`, and `agent-table.json` at `openspec/changes/implement-phase-b-motion-lifecycle/verification/checkpoint-4/agent-table.json`. State that usable active agents are resumed first, new agents are spawned only when no usable active agent exists, only `block -> pass` marks an agent `non_active`, and termination depends on a valid active pass.
