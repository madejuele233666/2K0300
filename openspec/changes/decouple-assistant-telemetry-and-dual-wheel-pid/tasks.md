## 0. Verification Contract

- Shared sequence:
  - `openspec/schemas/ai-enforced-workflow/verification-sequence.md#verify-sequence/default`
- Shared JSON verification contract:
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json`
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json`
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-agent-table-v1.json`
- Cache-helper sequence:
  - `openspec/schemas/ai-enforced-workflow/index-sequence.md#index-sequence/default`
- Shared field groups:
  - `invocation_common_required`
  - `output_paths_required`
  - `verifier_evidence_required`
  - `valid_pass_requirements`
  - `partial_scope_rule`
- Routing target for blocking findings:
  - `openspec-repair-change`
- Supported continuation overrides:
  - `verify-only`
  - `dry-run`
  - `manual_pause`
- Artifact-completion gate ownership:
  - when this task list completes the schema's `applyRequires` set under `ai-enforced-workflow`, the active artifact-creation caller (`openspec-propose`, `openspec-ff-change`, or `openspec-continue-change`) runs docs-first review before implementation entry
  - `openspec-apply-change` does not own that docs-first artifact gate

## 1. Assistant Sidecar Boundary And Control Snapshot

- [x] 1.1 Add a project-owned control debug snapshot boundary under `new/code/runtime/` and extend `new/code/runtime/runtime_state.hpp` plus `new/code/runtime/control_loop.cpp` so the runtime publishes motion phase, veto reason, effective speed target, left/right targets, left/right measured speed, turn correction, and left/right PWM command through one snapshot API instead of exposing timer locals or vendor state.
- [x] 1.2 Add a project-owned non-assistant control evidence exporter under `new/code/runtime/` or an equivalent project-owned diagnostics/harness helper so `ControlDebugSnapshot` can be emitted as structured markers, structured logs, or harness-visible exports during assistant-disabled verification.
- [x] 1.3 Add a project-owned assistant sidecar stack using `new/code/runtime/assistant_service.*`, `new/code/platform/assistant_link.*`, and `new/code/platform/true_ls2k0300/assistant_bridge.*` so Seekfree Assistant transport/protocol ownership stays outside `legacy` and `runtime`, the transport boundary reserves future extension while implementing TCP only, and use `openspec-align` against the E07 TCP/oscilloscope plus E07_04 image-transfer examples only to preserve protocol entrypoints without copying vendor structure into the public control surface.
- [x] 1.4 Update `new/user/main.cpp` so assistant sidecar initialization, polling, read-only waveform publication, and read-only image publication run in the foreground loop, remain optional/non-blocking, and never change `startup.complete`, `control.start`, or `motion_supervisor` semantics when the sidecar is absent or unhealthy.
- [x] 1.5 Update `new/user/CMakeLists.txt` so assistant sidecar files are built from `new/` while vendor assistant sources come only from `true_LS2K0300_Library/Seekfree_LS2K0300_Opensource_Library`, and verify that the main runtime still builds and runs with assistant sidecar disabled.
- [x] 1.6 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the assistant sidecar boundary and control snapshot outputs (`new/code/runtime/assistant_service.*`, `new/code/runtime/control_debug_snapshot.*` or equivalent, the non-assistant evidence exporter, `new/code/platform/assistant_link.*`, `new/code/platform/true_ls2k0300/assistant_bridge.*`, `new/user/main.cpp`, `new/user/CMakeLists.txt`). Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json` and `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json` for `invocation_common_required`, `output_paths_required`, `verifier_evidence_required`, `subject_required_any_of`, `findings_required`, `finding_object_required`, `finding_semantics`, `repair_routing_rules`, `valid_pass_requirements`, and `partial_scope_rule`, plus the routing target and the caller-local guardrail that assistant integration must remain optional, TCP-only for this release, and vendor-isolated while wheel-level evidence remains available without assistant connectivity. Require authoritative verifier findings JSON at `openspec/changes/decouple-assistant-telemetry-and-dual-wheel-pid/verification/checkpoint-2/attempt-<n>/findings.json`, verifier execution evidence JSON at `openspec/changes/decouple-assistant-telemetry-and-dual-wheel-pid/verification/checkpoint-2/attempt-<n>/verifier-evidence.json`, and `agent-table.json` at `openspec/changes/decouple-assistant-telemetry-and-dual-wheel-pid/verification/checkpoint-2/agent-table.json`. State that usable active agents are continued first, that callers prefer `send_input` while the same active agent is still open, that `send_input` returning `agent not found` still routes to `resume`, not spawn, that callers use `resume` only when that same active agent was closed and must be restored, that new agents are spawned only when no usable active agent exists, that `no_usable_active_agent` means only that `agent-table.json` literally contains no `active` agent, that recovery spawns keep distinct reason codes such as `active_agent_missing` or `active_agent_not_resumable`, that `spawn_active` decisions expose machine-readable `spawn_reason_code`, that only `block -> pass` marks an agent `non_active`, and that termination depends on a valid active pass. If the checkpoint explicitly enables Gemini dual review, require logical runner contract `gemini-capture`, raw plus normalized output, and recovery via `input_raw_path -> report_path`.

## 2. Dual-Wheel Target Generation And Independent Wheel PID

- [x] 2.1 Add `new/code/legacy/wheel_target_mixer.*` so `new/` computes `left_speed_target` and `right_speed_target` from lifecycle-owned `effective_speed_target` plus bounded turn correction, instead of treating `turn_output` only as a final PWM bias.
- [x] 2.2 Add `new/code/legacy/wheel_pid.*` with distinct left and right controller state, then refactor `new/code/legacy/pid_control.*` and `new/code/legacy/motor_logic.*` so the main speed path no longer depends on `ComputeMeanSpeedPwm(...) + Mix(mean_pwm, turn_output)` as the authoritative wheel-output implementation.
- [x] 2.3 Rewrite the command-generation slice in `new/code/runtime/control_loop.cpp` so gate/lifecycle semantics remain unchanged, but the drive-allowed path becomes `effective_speed_target -> wheel targets -> left/right PID -> ActuatorCommand{left_pwm,right_pwm,emergency_stop}` while preserving STOPPING, hold-disarmed, and emergency-stop behavior.
- [x] 2.4 Keep normalized left/right wheel semantics at the project boundary by leaving sign normalization and vendor side mapping in `new/code/platform/encoder_adapter.cpp`, `new/code/platform/motor_adapter.cpp`, and `new/code/platform/true_ls2k0300/*`, and audit that the new wheel controller path consumes only logical left/right values.
- [x] 2.5 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the dual-wheel control path (`new/code/legacy/wheel_target_mixer.*`, `new/code/legacy/wheel_pid.*`, `new/code/legacy/pid_control.*`, `new/code/legacy/motor_logic.*`, `new/code/runtime/control_loop.cpp`, and any touched adapter normalization files). Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json` and `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json` for `invocation_common_required`, `output_paths_required`, `verifier_evidence_required`, `subject_required_any_of`, `findings_required`, `finding_object_required`, `finding_semantics`, `repair_routing_rules`, `valid_pass_requirements`, and `partial_scope_rule`, plus the routing target and the caller-local guardrail that public actuator contract and lifecycle semantics must remain unchanged while internal wheel control is refactored and left/right PID parameters remain explicitly independent. Require authoritative verifier findings JSON at `openspec/changes/decouple-assistant-telemetry-and-dual-wheel-pid/verification/checkpoint-3/attempt-<n>/findings.json`, verifier execution evidence JSON at `openspec/changes/decouple-assistant-telemetry-and-dual-wheel-pid/verification/checkpoint-3/attempt-<n>/verifier-evidence.json`, and `agent-table.json` at `openspec/changes/decouple-assistant-telemetry-and-dual-wheel-pid/verification/checkpoint-3/agent-table.json`. State that usable active agents are continued first, that callers prefer `send_input` while the same active agent is still open, that `send_input` returning `agent not found` still routes to `resume`, not spawn, that callers use `resume` only when that same active agent was closed and must be restored, that new agents are spawned only when no usable active agent exists, that `no_usable_active_agent` means only that `agent-table.json` literally contains no `active` agent, that recovery spawns keep distinct reason codes such as `active_agent_missing` or `active_agent_not_resumable`, that `spawn_active` decisions expose machine-readable `spawn_reason_code`, that only `block -> pass` marks an agent `non_active`, and that termination depends on a valid active pass. If the checkpoint explicitly enables Gemini dual review, require logical runner contract `gemini-capture`, raw plus normalized output, and recovery via `input_raw_path -> report_path`.

## 3. Parameter Surface, Build Wiring, And Phase B Verification Surface

- [x] 3.1 Extend `new/code/port/control_types.hpp`, `new/code/platform/param_store.cpp`, and `new/config/default_params.json` with the dual-wheel and sidecar-related runtime fields needed by the design, including explicit left/right wheel PID parameter blocks with copied initial baseline values, turn-to-wheel-target conversion parameters, sidecar enablement defaults, and TCP/image-publication configuration fields.
- [x] 3.2 Update `new/docs/race-finish-series.zh-CN/02-phase-b-low-speed-vehicle-motion.md`, `new/docs/race-finish-series.zh-CN/07-current-progress.md`, and directly impacted verification notes so Phase B evidence and operator guidance explicitly use left/right targets, left/right feedback, left/right PWM, assistant-disabled structured snapshot export, and optional assistant wave/image outputs instead of relying only on mean speed or mixed PWM interpretation.
- [x] 3.3 Add or update Phase B verification notes under `new/verification/` or this change-local verification bundle so bench-safe assistant-disabled runs, assistant-enabled waveform runs, assistant-enabled image-transfer runs, and low-speed wheel-control runs each state the exact command/env surface used, the expected markers or exports, and what the evidence proves versus what it does not prove.
- [x] 3.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the parameter/build/doc/evidence surface (`new/code/port/control_types.hpp`, `new/code/platform/param_store.cpp`, `new/config/default_params.json`, `new/user/CMakeLists.txt`, touched Phase B docs, and updated verification notes). Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json` and `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json` for `invocation_common_required`, `output_paths_required`, `verifier_evidence_required`, `subject_required_any_of`, `findings_required`, `finding_object_required`, `finding_semantics`, `repair_routing_rules`, `valid_pass_requirements`, and `partial_scope_rule`, plus the routing target and the caller-local guardrail that assistant wave/image outputs remain optional support evidence while wheel-level observability becomes primary Phase B evidence through a project-owned non-assistant export surface. Require authoritative verifier findings JSON at `openspec/changes/decouple-assistant-telemetry-and-dual-wheel-pid/verification/checkpoint-4/attempt-<n>/findings.json`, verifier execution evidence JSON at `openspec/changes/decouple-assistant-telemetry-and-dual-wheel-pid/verification/checkpoint-4/attempt-<n>/verifier-evidence.json`, and `agent-table.json` at `openspec/changes/decouple-assistant-telemetry-and-dual-wheel-pid/verification/checkpoint-4/agent-table.json`. State that usable active agents are continued first, that callers prefer `send_input` while the same active agent is still open, that `send_input` returning `agent not found` still routes to `resume`, not spawn, that callers use `resume` only when that same active agent was closed and must be restored, that new agents are spawned only when no usable active agent exists, that `no_usable_active_agent` means only that `agent-table.json` literally contains no `active` agent, that recovery spawns keep distinct reason codes such as `active_agent_missing` or `active_agent_not_resumable`, that `spawn_active` decisions expose machine-readable `spawn_reason_code`, that only `block -> pass` marks an agent `non_active`, and that termination depends on a valid active pass. If the checkpoint explicitly enables Gemini dual review, require logical runner contract `gemini-capture`, raw plus normalized output, and recovery via `input_raw_path -> report_path`.

## 4. Final Change Verification

- [ ] 4.1 Use `openspec-verify-change` after implementation to run source-first review for the full changed surface, keeping `review_goal=implementation_correctness`, preserving the same verifier-subagent continuation semantics, and writing authoritative findings/evidence into this change-local verification tree.
- [ ] 4.2 Produce final implementation evidence for at least one assistant-disabled Phase B run, one assistant-enabled waveform run, one assistant-enabled image-transfer run, plus the bench-safe or low-speed wheel-control evidence needed to show left/right target generation and left/right command application are interpretable.
- [ ] 4.3 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the final changed source surface and implementation evidence bundle. Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json` and `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json` for `invocation_common_required`, `output_paths_required`, `verifier_evidence_required`, `subject_required_any_of`, `findings_required`, `finding_object_required`, `finding_semantics`, `repair_routing_rules`, `valid_pass_requirements`, and `partial_scope_rule`, plus the routing target and the caller-local guardrail that the final verdict must confirm both assistant sidecar isolation and dual-wheel control correctness without reopening lifecycle/public-actuator contracts. Require authoritative verifier findings JSON at `openspec/changes/decouple-assistant-telemetry-and-dual-wheel-pid/verification/checkpoint-5/attempt-<n>/findings.json`, verifier execution evidence JSON at `openspec/changes/decouple-assistant-telemetry-and-dual-wheel-pid/verification/checkpoint-5/attempt-<n>/verifier-evidence.json`, and `agent-table.json` at `openspec/changes/decouple-assistant-telemetry-and-dual-wheel-pid/verification/checkpoint-5/agent-table.json`. State that usable active agents are continued first, that callers prefer `send_input` while the same active agent is still open, that `send_input` returning `agent not found` still routes to `resume`, not spawn, that callers use `resume` only when that same active agent was closed and must be restored, that new agents are spawned only when no usable active agent exists, that `no_usable_active_agent` means only that `agent-table.json` literally contains no `active` agent, that recovery spawns keep distinct reason codes such as `active_agent_missing` or `active_agent_not_resumable`, that `spawn_active` decisions expose machine-readable `spawn_reason_code`, that only `block -> pass` marks an agent `non_active`, and that termination depends on a valid active pass. If the checkpoint explicitly enables Gemini dual review, require logical runner contract `gemini-capture`, raw plus normalized output, and recovery via `input_raw_path -> report_path`.
