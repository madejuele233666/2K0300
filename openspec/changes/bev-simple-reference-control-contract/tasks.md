# Tasks

- [x] Rename reference history state to hold-memory terminology.
- [x] Remove obsolete telemetry labels from active C++ types and JSON encoders.
- [x] Split reference-control readiness from safety gate ownership.
- [x] Move Otsu thresholding out of the removed camera-logic facade and compose layers in the runtime frontend.
- [x] Rename active speed/yaw PID parameters to `RUNNING_SPEED_TARGET` and `YAW_RATE_PID`.
- [x] Remove active roadblock debug placeholders.
- [x] Remove unused BEV geometry parameters from types, JSON defaults, parameter loading, and media config snapshots.
- [x] Update host tools and selftests to consume only the minimal steering contract.
- [x] Rewrite parameter documentation and this change package to describe the simple BEV pipeline.
- [x] Run the minimal regression commands, host build, diff check, code-index refresh, and active residual scan.
