## 0. Verification Contract

- Shared sequence:
  - `openspec/schemas/ai-enforced-workflow/verification-sequence.md#verify-sequence/default`
- Shared JSON verification contract:
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json`
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json`
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-agent-table-v1.json`
- Cache-helper sequence:
  - `openspec/schemas/ai-enforced-workflow/index-sequence.md#index-sequence/default`
- Shared field groups:
  - `invocation_common_required`
  - `output_paths_required`
  - `verifier_evidence_required`
  - `valid_pass_requirements`
  - `partial_scope_rule`
- Routing target for blocking findings:
  - `openspec-repair-change`
- Supported continuation overrides:
  - `verify-only`
  - `dry-run`
  - `manual_pause`
- Artifact-completion gate ownership:
  - when this task list completes the schema's `applyRequires` set under
    `ai-enforced-workflow`, the active artifact-creation caller
    (`openspec-propose` or `openspec-continue-change`)
    runs docs-first review before implementation entry
  - `openspec-apply-change` does not own that docs-first artifact gate

## 1. Bidirectional Assistant Command Surface

- [x] 1.1 Add a project-owned inbound command contract and decoder above the existing TCP bridge, including typed commands for `start`, `stop`, tuning-mode enable/disable, turn-suppression control, and target-speed override with `seq` request correlation, TTL expiry handling for overrides, and `set_target_speed.value` validation against the accepted first-release numeric domain and range.
- [x] 1.2 Extend `assistant_bridge` / `assistant_link` / `assistant_service` so assistant receive traffic is surfaced to the project-owned decoder, malformed or unsupported payloads are rejected through the frozen standalone `input_rejected` state path with a human-readable reason string, and assistant send/receive handling stays outside the control timer.
- [x] 1.3 Add runtime-owned ACK/state feedback for the accepted command surface, using `accepted`/`rejected` outcome with reason string for ACK frames and `input_rejected`/`override_cleared`/`snapshot_cleared` events for standalone state frames, with frozen number-or-`null` override encoding and phase-based `effective_speed_target` encoding, so host tooling does not infer acceptance only from wheel-speed changes.
- [x] 1.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the bidirectional assistant command boundary, transport/decoder split, and ACK/state feedback contract. Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json` and `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json` for `invocation_common_required`, `output_paths_required`, `verifier_evidence_required`, `subject_required_any_of`, `findings_required`, `finding_object_required`, `finding_semantics`, `repair_routing_rules`, `valid_pass_requirements`, and `partial_scope_rule`, plus the routing target and caller-local guardrail that raw TCP buffers and vendor payloads must not mutate runtime state directly. Require authoritative verifier findings JSON, verifier execution evidence JSON, and `agent-table.json` at `openspec/changes/add-bidirectional-assistant-speed-tuning/verification/checkpoint-1/attempt-<n>/findings.json`, `openspec/changes/add-bidirectional-assistant-speed-tuning/verification/checkpoint-1/attempt-<n>/verifier-evidence.json`, and `openspec/changes/add-bidirectional-assistant-speed-tuning/verification/checkpoint-1/agent-table.json`. State that usable active agents are continued first, that callers prefer `send_input` while the same active agent is still open, that `send_input` returning `agent not found` still routes to `resume`, not spawn, that callers use `resume` only when that same active agent was closed and must be restored, that new agents are spawned only when no usable active agent exists, that `no_usable_active_agent` means only that `agent-table.json` literally contains no `active` agent, that recovery spawns keep distinct reason codes such as `active_agent_missing` or `active_agent_not_resumable`, that `spawn_active` decisions expose machine-readable `spawn_reason_code`, that only `block -> pass` marks an agent `non_active`, and that termination depends on a valid active pass.

## 2. Runtime Tuning State And Lifecycle Integration

- [x] 2.1 Add a dedicated runtime tuning-state module and shared snapshot flow that keeps tuning mode, target-speed override, turn-suppression state, optional session-local request-correlation bookkeeping, and TTL-expiry logic separate from startup-loaded params and persisted PID configuration, with full-snapshot clear occurring on `disable_tuning_mode` or disconnect rather than `stop` alone.
- [x] 2.2 Wire remote `start` / `stop` commands into the existing motion-intent boundary so the Phase B lifecycle remains owned by `motion_supervisor`, and add diagnostics proving remote operator intent does not write lifecycle phase or actuator output directly.
- [x] 2.3 Update `control_loop` and related runtime-owned snapshot surfaces so target-speed override feeds lifecycle-owned running-speed selection, tuning-only turn suppression zeroes only the applied turn path, raw-versus-applied turn remains observable, and disconnect/timeout/tuning-disable clear the volatile tuning state cleanly.
- [x] 2.4 Exercise lifecycle and control-path safety with focused runtime tests or smoke runs covering: blocked remote start, shaped remote stop, override TTL expiry, assistant disconnect clearing tuning state, tuning-mode-off normal runs, remote `start`/`stop` rejection while `FAIL_SAFE_LATCHED`, and fail-safe behavior remaining authoritative while tuning mode is enabled.
- [x] 2.5 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for runtime tuning-state isolation, lifecycle-intent routing, and applied-turn suppression behavior. Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json` and `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json` for `invocation_common_required`, `output_paths_required`, `verifier_evidence_required`, `subject_required_any_of`, `findings_required`, `finding_object_required`, `finding_semantics`, `repair_routing_rules`, `valid_pass_requirements`, and `partial_scope_rule`, plus the routing target and caller-local guardrails that tuning mode must not rewrite persisted params, must not create a parallel lifecycle, and must suppress only applied turn output. Require authoritative verifier findings JSON, verifier execution evidence JSON, and `agent-table.json` at `openspec/changes/add-bidirectional-assistant-speed-tuning/verification/checkpoint-2/attempt-<n>/findings.json`, `openspec/changes/add-bidirectional-assistant-speed-tuning/verification/checkpoint-2/attempt-<n>/verifier-evidence.json`, and `openspec/changes/add-bidirectional-assistant-speed-tuning/verification/checkpoint-2/agent-table.json`. State that usable active agents are continued first, that callers prefer `send_input` while the same active agent is still open, that `send_input` returning `agent not found` still routes to `resume`, not spawn, that callers use `resume` only when that same active agent was closed and must be restored, that new agents are spawned only when no usable active agent exists, that `no_usable_active_agent` means only that `agent-table.json` literally contains no `active` agent, that recovery spawns keep distinct reason codes such as `active_agent_missing` or `active_agent_not_resumable`, that `spawn_active` decisions expose machine-readable `spawn_reason_code`, that only `block -> pass` marks an agent `non_active`, and that termination depends on a valid active pass.

## 3. Structured Telemetry And Host Tuning Workflow

- [x] 3.1 Extend project-owned observability so tuning runs export left/right target, left/right measured speed, left/right PWM, raw turn, applied turn, tuning-mode flags, override state, and motion phase through both assistant-compatible publication and structured host-oriented telemetry.
- [x] 3.2 Add the minimal host tuning workflow in `new/user/` with Python + `matplotlib`, including package-install fallback when plotting support is absent, live left/right target-vs-measured plots, command send support, CSV capture for repeated PID tuning runs, and support for the frozen newline-delimited JSON command/ACK/state/telemetry schema with explicit disabled-mode rejection handling, invalid-target-speed rejection handling, parsing of standalone `input_rejected`, `override_cleared`, and `snapshot_cleared` state frames, and direct handling of `target_speed_override_value=null` plus phase-based `effective_speed_target`.
- [x] 3.3 Update `new/user/debug.sh`, README/runbooks, and verification docs so the accepted workflow covers assistant setup, tuning-mode enablement, remote start/stop, repeated target-speed sequences, CSV/log artifact capture, and the rule that PID changes still use JSON params plus restart.
- [x] 3.4 Exercise the host workflow end-to-end with the minimal accepted tuning sequence (`enable_tuning_mode -> start -> set_target_speed steps -> stop -> disable_tuning_mode`), requiring the preserved evidence to show exactly one full-snapshot clear on `disable_tuning_mode` for that sequence, one explicit rejected-command case where `set_target_speed` is sent while tuning mode is off, one explicit rejected-command case where `set_turn_suppressed` is sent while tuning mode is off, and at least one invalid-target-speed case that proves the runtime rejects out-of-contract override values without mutating the active tuning snapshot; preserve evidence showing command acceptance, rejection semantics, standalone `input_rejected`/clear-event state frames, frozen non-driving field encodings, live telemetry, CSV output, and plotting/install fallback behavior.
- [x] 3.5 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for structured telemetry, host tuning workflow, and runbook evidence. Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json` and `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json` for `invocation_common_required`, `output_paths_required`, `verifier_evidence_required`, `subject_required_any_of`, `findings_required`, `finding_object_required`, `finding_semantics`, `repair_routing_rules`, `valid_pass_requirements`, and `partial_scope_rule`, plus the routing target and caller-local guardrails that host plotting is minimal, installation fallback must not redefine the board protocol, and CSV capture remains available even when plotting is unavailable. Require authoritative verifier findings JSON, verifier execution evidence JSON, and `agent-table.json` at `openspec/changes/add-bidirectional-assistant-speed-tuning/verification/checkpoint-3/attempt-<n>/findings.json`, `openspec/changes/add-bidirectional-assistant-speed-tuning/verification/checkpoint-3/attempt-<n>/verifier-evidence.json`, and `openspec/changes/add-bidirectional-assistant-speed-tuning/verification/checkpoint-3/agent-table.json`. State that usable active agents are continued first, that callers prefer `send_input` while the same active agent is still open, that `send_input` returning `agent not found` still routes to `resume`, not spawn, that callers use `resume` only when that same active agent was closed and must be restored, that new agents are spawned only when no usable active agent exists, that `no_usable_active_agent` means only that `agent-table.json` literally contains no `active` agent, that recovery spawns keep distinct reason codes such as `active_agent_missing` or `active_agent_not_resumable`, that `spawn_active` decisions expose machine-readable `spawn_reason_code`, that only `block -> pass` marks an agent `non_active`, and that termination depends on a valid active pass.

## 4. Final Verification And Implementation Entry

- [x] 4.1 Run source-first verification for the completed change using `openspec-verify-change`, covering the changed assistant transport/runtime/control/user-tooling surfaces, the documented smoke/tuning exercises, and the preserved lifecycle/fail-safe boundaries.
- [x] 4.2 Preserve implementation review artifacts under `openspec/changes/add-bidirectional-assistant-speed-tuning/verification/checkpoint-4/` with authoritative findings JSON, verifier execution evidence JSON, and `agent-table.json`, and route any blocking findings to `openspec-repair-change`.
- [x] 4.3 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the final source-first implementation bundle. Use repository-index cache helper `index-sequence/default` only when cache discovery or refresh is useful; do not require cache authority before review. Reference the shared field groups in `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json` and `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json` for `invocation_common_required`, `output_paths_required`, `verifier_evidence_required`, `subject_required_any_of`, `findings_required`, `finding_object_required`, `finding_semantics`, `repair_routing_rules`, `valid_pass_requirements`, and `partial_scope_rule`, plus the routing target and caller-local guardrails from this change. Require authoritative verifier findings JSON, verifier execution evidence JSON, and `agent-table.json` at `openspec/changes/add-bidirectional-assistant-speed-tuning/verification/checkpoint-4/attempt-<n>/findings.json`, `openspec/changes/add-bidirectional-assistant-speed-tuning/verification/checkpoint-4/attempt-<n>/verifier-evidence.json`, and `openspec/changes/add-bidirectional-assistant-speed-tuning/verification/checkpoint-4/agent-table.json`. Follow `cycle_rules` in `verification-cycle-core-v1.json` for agent lifecycle.
