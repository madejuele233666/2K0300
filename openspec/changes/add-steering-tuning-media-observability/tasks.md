## 0. Verification Contract

- Shared sequence:
  - `openspec/schemas/ai-enforced-workflow/verification-sequence.md#verify-sequence/default`
- Shared JSON verification contract:
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-core-v1.json`
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-openspec-adapter-v1.json`
  - `openspec/schemas/modules/verification-cycle/contracts/verification-cycle-agent-table-v1.json`
- Shared field groups:
  - `invocation_common_required`
  - `output_paths_required`
  - `verifier_evidence_required`
  - `valid_pass_requirements`
  - `partial_scope_rule`
- Routing target for blocking findings:
  - `openspec-repair-change`
- Supported continuation overrides:
  - `verify-only`
  - `dry-run`
  - `manual_pause`
- Artifact-completion gate ownership:
  - when this task list completes the schema's `applyRequires` set under
    `ai-enforced-workflow`, the active artifact-creation caller
    (`openspec-propose` or `openspec-continue-change`)
    runs docs-first review before implementation entry
  - `openspec-apply-change` does not own that docs-first artifact gate

## 1. Steering Snapshot And Non-Assistant Evidence

- [ ] 1.1 Extend the runtime-owned control snapshot family so one accepted steering snapshot carries `lateral_error`, `highest_line`, `threshold`, `threshold_veto`, `resolved_fuzzy_p`, `camera_p_term`, `camera_d_term`, `w_target`, `gyro_z`, `gyro_error`, `gyro_p_term`, `gyro_d_term`, `raw_turn_output`, and `applied_turn_output` without requiring host-side recomputation.
- [ ] 1.2 Add a project-owned non-assistant export surface such as `control.steering_snapshot`, wired from the same runtime-owned steering snapshot family, so steering observability can be accepted while assistant and steering media are disabled.
- [ ] 1.3 Exercise the snapshot/export surface with focused local runtime tests or diagnostics captures that prove assistant-disabled evidence still exposes the accepted steering fields.
- [ ] 1.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the steering snapshot surface and non-assistant export contract. Reference field groups in `verification-cycle-core-v1.json` and `verification-cycle-openspec-adapter-v1.json`. Follow `cycle_rules` for agent lifecycle. Require authoritative findings JSON, verifier evidence JSON, and `agent-table.json` at `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-1/attempt-<n>/findings.json`, `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-1/attempt-<n>/verifier-evidence.json`, and `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-1/agent-table.json`. State that usable active agents are continued first, that callers prefer `send_input` while the same active agent is still open, that `send_input` returning `agent not found` still routes to `resume`, not spawn, that callers use `resume` only when that same active agent was closed and must be restored, that new agents are spawned only when no usable active agent exists, that `no_usable_active_agent` means only that `agent-table.json` literally contains no `active` agent, that recovery spawns keep distinct reason codes such as `active_agent_missing` or `active_agent_not_resumable`, that `spawn_active` decisions expose machine-readable `spawn_reason_code`, that only `block -> pass` marks an agent `non_active`, and that termination depends on a valid active pass.

## 2. Media Transport, Protocol, And Configuration

- [ ] 2.1 Add a project-owned steering media transport boundary and protocol module that defines one length-prefixed binary envelope plus first-release frame families `config_snapshot` and `image_frame`, with a frozen JSON header object shape for each frame family, `config_snapshot.param_snapshot` carrying the accepted read-only whitelist keys, `media_publish_interval_ms` as the accepted cadence field, and `image_frame.steering_snapshot` carrying the accepted steering metadata.
- [ ] 2.2 Extend the platform-owned TCP bridge and startup-loaded parameter surface so the board can actively connect to the host for a separate steering media session that shares `assistant_tcp.host`, uses `steering_media_enabled`, `steering_media_port`, and `steering_media_publish_interval_ms` as the accepted board-side configuration keys, and never becomes a command-bearing channel.
- [ ] 2.3 Add focused protocol and transport tests covering header/payload length validation, `config_snapshot` send-on-ready behavior, `image_frame` payload-size checks, and busy-socket latest-frame drop semantics.
- [ ] 2.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the steering media transport and protocol contract. Reference field groups in `verification-cycle-core-v1.json` and `verification-cycle-openspec-adapter-v1.json`. Follow `cycle_rules` for agent lifecycle. Require authoritative findings JSON, verifier evidence JSON, and `agent-table.json` at `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-2/attempt-<n>/findings.json`, `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-2/attempt-<n>/verifier-evidence.json`, and `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-2/agent-table.json`. State that usable active agents are continued first, that callers prefer `send_input` while the same active agent is still open, that `send_input` returning `agent not found` still routes to `resume`, not spawn, that callers use `resume` only when that same active agent was closed and must be restored, that new agents are spawned only when no usable active agent exists, that `no_usable_active_agent` means only that `agent-table.json` literally contains no `active` agent, that recovery spawns keep distinct reason codes such as `active_agent_missing` or `active_agent_not_resumable`, that `spawn_active` decisions expose machine-readable `spawn_reason_code`, that only `block -> pass` marks an agent `non_active`, and that termination depends on a valid active pass.

## 3. Runtime Publish Path And Control Regression

- [ ] 3.1 Integrate steering media publication into the foreground sidecar/runtime publish path so media readiness triggers one `config_snapshot`, `image_frame` headers consume the accepted steering snapshot, and media publish intervals remain configurable through startup-loaded params.
- [ ] 3.2 Preserve the existing assistant JSON control contract and regression-proof `command/ack/state/telemetry`, including `input_rejected`, `override_cleared`, `snapshot_cleared`, disconnect clear behavior, and `FAIL_SAFE_LATCHED` rejection semantics while steering media is enabled.
- [ ] 3.3 Run focused runtime and board smoke exercises showing that control and steering media sessions can coexist, that active tuning sessions keep receiving steering image/metadata, and that media disconnect or congestion does not break the accepted control cleanup path.
- [ ] 3.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the runtime dual-channel publish path and control regression bundle. Reference field groups in `verification-cycle-core-v1.json` and `verification-cycle-openspec-adapter-v1.json`. Follow `cycle_rules` for agent lifecycle. Require authoritative findings JSON, verifier evidence JSON, and `agent-table.json` at `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-3/attempt-<n>/findings.json`, `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-3/attempt-<n>/verifier-evidence.json`, and `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-3/agent-table.json`. State that usable active agents are continued first, that callers prefer `send_input` while the same active agent is still open, that `send_input` returning `agent not found` still routes to `resume`, not spawn, that callers use `resume` only when that same active agent was closed and must be restored, that new agents are spawned only when no usable active agent exists, that `no_usable_active_agent` means only that `agent-table.json` literally contains no `active` agent, that recovery spawns keep distinct reason codes such as `active_agent_missing` or `active_agent_not_resumable`, that `spawn_active` decisions expose machine-readable `spawn_reason_code`, that only `block -> pass` marks an agent `non_active`, and that termination depends on a valid active pass.

## 4. Host Workflow Extension

- [ ] 4.1 Extend `new/user/debug.sh tuning ...`, `debug.sh assistant local`, and the accepted Python host workflow so they can configure/listen for the accepted steering media port, update the board-side control/media wiring keys through the project-owned setup surface, record raw image frames and metadata, and align those artifacts with the existing control CSV and host log outputs.
- [ ] 4.2 Preserve first-release scope by keeping steering observability read-only: capture the accepted parameter whitelist in `config_snapshot`, but do not add steering-parameter write commands, param-file mutation, or a second ad hoc tuning entrypoint.
- [ ] 4.3 Update `new/user/README.md`, `new/docs/race-finish-series.zh-CN/04-phase-d-track-operations-and-tuning.md`, and related runbooks so the accepted workflow documents the frozen board-side control/media wiring keys, dual-channel setup, control/media alignment, and the rule that steering `P/D` changes still use parameter files plus restart.
- [ ] 4.4 Exercise the accepted host workflow end to end with steering media enabled, preserving a minimal evidence bundle containing board log, host CSV, media records, and time-alignment metadata or summary.
- [ ] 4.5 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the host workflow extension and evidence bundle. Reference field groups in `verification-cycle-core-v1.json` and `verification-cycle-openspec-adapter-v1.json`. Follow `cycle_rules` for agent lifecycle. Require authoritative findings JSON, verifier evidence JSON, and `agent-table.json` at `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-4/attempt-<n>/findings.json`, `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-4/attempt-<n>/verifier-evidence.json`, and `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-4/agent-table.json`. State that usable active agents are continued first, that callers prefer `send_input` while the same active agent is still open, that `send_input` returning `agent not found` still routes to `resume`, not spawn, that callers use `resume` only when that same active agent was closed and must be restored, that new agents are spawned only when no usable active agent exists, that `no_usable_active_agent` means only that `agent-table.json` literally contains no `active` agent, that recovery spawns keep distinct reason codes such as `active_agent_missing` or `active_agent_not_resumable`, that `spawn_active` decisions expose machine-readable `spawn_reason_code`, that only `block -> pass` marks an agent `non_active`, and that termination depends on a valid active pass.

## 5. Final Board Verification And Closeout

- [ ] 5.1 Run source-first verification for the completed change using `openspec-verify-change`, covering changed platform/runtime/user-tooling surfaces, local protocol tests, board smoke evidence, and the preserved control/media alignment bundle.
- [ ] 5.2 Preserve final verification artifacts under `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-5/` with authoritative findings JSON, verifier execution evidence JSON, and `agent-table.json`, routing any blocking findings to `openspec-repair-change`.
- [ ] 5.3 Capture a board smoke-test package proving simultaneous control/media connectivity, sustained image-plus-steering metadata delivery during an active tuning session, intact ACK/state behavior under media load, and unchanged stop/disable/disconnect cleanup semantics.
- [ ] 5.4 [Checkpoint] Run verifier-subagent review using `verify-sequence/default` for the final source-first implementation bundle. Reference field groups in `verification-cycle-core-v1.json` and `verification-cycle-openspec-adapter-v1.json`. Follow `cycle_rules` for agent lifecycle. Require authoritative findings JSON, verifier evidence JSON, and `agent-table.json` at `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-5/attempt-<n>/findings.json`, `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-5/attempt-<n>/verifier-evidence.json`, and `openspec/changes/add-steering-tuning-media-observability/verification/checkpoint-5/agent-table.json`. State that usable active agents are continued first, that callers prefer `send_input` while the same active agent is still open, that `send_input` returning `agent not found` still routes to `resume`, not spawn, that callers use `resume` only when that same active agent was closed and must be restored, that new agents are spawned only when no usable active agent exists, that `no_usable_active_agent` means only that `agent-table.json` literally contains no `active` agent, that recovery spawns keep distinct reason codes such as `active_agent_missing` or `active_agent_not_resumable`, that `spawn_active` decisions expose machine-readable `spawn_reason_code`, that only `block -> pass` marks an agent `non_active`, and that termination depends on a valid active pass.
