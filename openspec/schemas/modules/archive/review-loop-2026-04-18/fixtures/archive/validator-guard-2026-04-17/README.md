# Validator-Guard Fixture Archive

This archive contains the caller-shaped run fixture that is still used by the
Stage A validator to mutate negative guard cases.

Archived fixture:

- `review-runs/openspec-artifact-gate-close`

Why archived:

- it is useful for automated guard mutation tests
- it is not the cleanest human-facing example of the shared review loop
- the standalone fixtures remain the active reference surface instead
