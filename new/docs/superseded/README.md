# Superseded New Workspace Docs

This directory contains historical planning documents that no longer define the active runtime, parameter, media, overlay, or verification contract.

Current authority:

- Root `README.md` for BEV extension rules.
- `new/docs/README.md` for the active documentation index.
- `new/config/default_params.md` for runtime parameters.
- `new/code/port/README.md` for type/include boundaries.
- `new/user/README.md` for operator workflow.

Preserved history:

- `race-finish-series.zh-CN/`: old race-finish phase roadmap.
- `race-finish-series-source/`: source material absorbed by the old roadmap.
- `temp/`: old draft plans.
- `review-artifacts/` and standalone files: historical review or gap-analysis notes.

Do not use these files as active implementation or verification authority.
