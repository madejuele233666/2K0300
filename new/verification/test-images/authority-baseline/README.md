# Authority Baseline Assets

This directory keeps raw authority input fixtures and historical diagnostic outputs.

Only raw `*.raw` fixtures may be used as current input samples for the simple reference/control contract.

The historical manifest, overlay, and txt outputs are diagnostic archives and must not be used as active authority, runtime, parameter, media, overlay, or verification evidence.
